
  # ICT Device Registry UI Design

  This is a code bundle for ICT Device Registry UI Design. The original project is available at https://www.figma.com/design/GgCS0l8G3egvldT9fNgMV8/ICT-Device-Registry-UI-Design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  